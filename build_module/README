HOST_X86:
    Platform related files!
    [configfile] makerules/plat.mk
                PLAT_CCFLAGS = [...]   The CCFLAGS is valid for all codes during compilation
                PLAT_LDFLAGS = [...]   The PLAT_LDFLAGS are valid during the links

    [dir] platform_release 
        in this folder you can run . utils/env.mk to set PATH and LD_LIBRARY_PATH that is Point to [dir]lib /  dir[test]

    [dir] lib rootfs/usr/lib
        Stores the library files generated by the compilation
    
    [dir] test rootfs/usr/bin
        Stores the generated executable applications



    
